# ProductRecommendationEngine
AI-Powered Product Recommendation System built using the Amazon Reviews dataset (1.7M interactions). Implements a full end-to-end recommender pipeline including preprocessing, time-aware splitting, collaborative filtering, hybrid LLM embeddings, ANN retrieval, and a Streamlit UI for personalized recommendations.
